name: hermes-unsupported
version: 0.9.0
description: "Phase 12 Hermes 不支持依赖夹具（Agent Loop / Gateway / 全局单例 / 内部模块）"
author: "OpenColorful Test"
entry: __init__.py
hooks:
  - pre_tool_call
  - on_session_end
requires_env:
  - HERMES_HOME
