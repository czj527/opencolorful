export function UnavailableSection() {
  return (
    <section className="settings-section unavailable-section" data-testid="settings-section-future">
      <h2>Profile / 记忆 / 多 Agent / 插件</h2>
      <p className="save-error">尚未启用。此功能将在后续阶段开放。</p>
    </section>
  );
}